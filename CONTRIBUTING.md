# 변경을 남기는 방법

기능 추가·오류 수정·구조 변경은 실제 작업 단위로 브랜치와 PR을 남깁니다. 문구 수정처럼 작은 변경은 필요에 맞게 간단히 처리합니다.

1. `main`에서 `feature/설명`, `fix/설명`, `refactor/설명` 브랜치를 만듭니다.
2. 필요한 기능을 수정하고 `python -m unittest discover -s tests -v`를 실행합니다.
3. 실제 변경 목적을 설명하는 커밋을 만듭니다.
4. PR에 **문제·변경 후 동작·확인한 검사·남은 한계**를 적습니다.
5. CI 결과와 변경 파일을 확인한 뒤 병합합니다.

동작이 달라지는 배포는 `VERSION`과 `CHANGELOG.md`도 갱신합니다. 이미 끝난 과거 작업의 Issue·커밋 날짜를 만들어 넣지 않습니다.

샘플은 가상 데이터만 사용합니다. 테스트는 외부 발송 API를 모의 응답으로 대체하며, 실제 운영 설정과 이력은 커밋하지 않습니다.
